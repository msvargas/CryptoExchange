import {
  API_BASE_URL,
  AUTHOR_NAME,
  AVATAR_PROFILE_URL,
  CHART_BASE_URL,
  GITHUB_PROFILE_URL,
} from 'react-native-dotenv';

const config = {
  locale: 'en-US',
  apiBaseUrl: API_BASE_URL,
  chartBaseUrl: CHART_BASE_URL,
  author: AUTHOR_NAME,
  avatarProfileUrl: AVATAR_PROFILE_URL,
  githubProfileUrl: GITHUB_PROFILE_URL,
};

export default config;
